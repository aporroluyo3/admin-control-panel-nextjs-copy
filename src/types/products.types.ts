type Product = {
  id: number;
  title: string;
  vendor: string;
  handle: string;
  status: string;
  price: string;
  weight_unit: string;
  sku: string;
  image_url: string;
  compare_at_price: string;
  admin_graphql_api_id: string;
};

export type { Product };
